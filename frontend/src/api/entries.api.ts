import httpClient from './httpClient.api';const ENDPOINT = '/entries';export const getEntries = async (active = 0) => {  const { data } = await httpClient.get(ENDPOINT, {    params: {      active    },  });  return { data: data.entries }} // \\wsl.localhost\Ubuntu\home\piconite\.nvm\versions\node\v22.21.1\binexport const getEntriesWithPage = (active = 0, page: number, itemsPerPage: number, sortBy: string[]) =>  httpClient.get(ENDPOINT, {    params: {      active,      page,      itemsPerPage,      sortBy: JSON.stringify(sortBy)    },  });export const addEntry = (data: never) => httpClient.post(ENDPOINT, data);export const updateEntry = (id: number, data: never) => httpClient.put(`${ENDPOINT}/${id}`, data);export const deleteEntry = (id: number) => httpClient.delete(`${ENDPOINT}/${id}`);