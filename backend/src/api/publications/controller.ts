import { prisma } from '../../index.js';import { Prisma } from '@prisma/client';export async function addPublication(title: string, text: string, active: boolean) {  return prisma.publication.create({    data: {      title,      text,      active,    },  });}export async function getPublications(active: boolean) {  // if active = 0 then all entries will be shown (there is no extra query), if = 1 only active entries will be shown  if(active) {    return prisma.publication.findMany({      where: { active: true }    })  } else {    return prisma.publication.findMany();  }}export async function updatePublication(publicationId: number, publication: { title: string; text: string; active: boolean }) {  try {    return await prisma.publication.update({      data: {        title: publication.title,        text: publication.text,        active: publication.active,      },      where: { id: publicationId }    });  } catch (e) {    if (e instanceof Prisma.PrismaClientKnownRequestError && e.code === "P2025") return null;    throw e;  }}export async function deletePublication(id: number) {  try {    return await prisma.publication.delete({      where: { id }    })  } catch (e) {    if (e instanceof Prisma.PrismaClientKnownRequestError && e.code === "P2025") return null;    throw e;  }}